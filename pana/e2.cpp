#include <algorithm>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <list>
#include <map>
#include <numeric>
#include <set>
#include <string>
#include <vector>

#define rep(i, x, n) for(int i = x; i < n; i++)
using namespace std;
const long long INF = 1LL << 60;



int main() {
string a,b,c;
cin>>a>>b>>c;
int as=a.size();
int bs=b.size();
int cs=c.size();
vector<string>ab;
ab.push_back(a+b);
ab.push_back(b+a);
rep(i,0,as){

}
}
