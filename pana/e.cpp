#include <algorithm>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <list>
#include <map>
#include <numeric>
#include <set>
#include <string>
#include <vector>

#define rep(i, x, n) for(int i = x; i < n; i++)
using namespace std;
const long long INF = 1LL << 60;



int main() {
string a,b,c;
cin>>a>>b>>c;
int as=a.size();
int bs=b.size();
int cs=c.size();
vector<string>ab;

rep(i,0,as){
    bool kabri=true;
    string reskouho="";
    rep(t,0,min(as,bs)){
        if(t<i){
            reskouho+=a[t];
        }
        if(a[t]=='?'){
            if(b[])
        }
        if(a[t]!='?'&&b[t]!='?'&&a[t]!=b[t]){
            kabri=false;
            break;
        }
    }
    if(kaburi){
        ab.push_back
    }
}
}
